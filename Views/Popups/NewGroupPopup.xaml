<?xml version="1.0" encoding="utf-8" ?>

<toolkit:Popup
    x:Class="Quadro_de_pendencias.Views.Popups.NewGroupPopup"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:models="clr-namespace:Quadro_de_pendencias.Models"
    x:TypeArguments="models:GroupModel"
    BackgroundColor="Transparent"
    Padding="0"
    >

    <Border
        Style="{StaticResource popupBorderStyle}">

        <VerticalStackLayout Spacing="20">
            <Label
                Text="Novo Grupo"
                Style="{StaticResource popupTitleLabelStyle}"/>

            <VerticalStackLayout Spacing="8">
                <Entry
                    Placeholder="Nome do grupo"
                    Text="{Binding Name}" />
            </VerticalStackLayout>

            <CollectionView
                ItemsSource="{Binding Colors}"
                SelectionMode="None"
                HorizontalScrollBarVisibility="Never">

                <CollectionView.ItemsLayout>
                    <LinearItemsLayout
                        Orientation="Horizontal"
                        ItemSpacing="12" />
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate>

                        <Border
                            WidthRequest="35"
                            HeightRequest="35"
                            Padding="3"
                            StrokeThickness="2"
                            StrokeShape="RoundRectangle 19">

                            <Border.Triggers>

                                <!-- Cor selecionada -->
                                <DataTrigger
                                    TargetType="Border"
                                    Binding="{Binding IsSelected}"
                                    Value="True">

                                    <Setter
                                        Property="Stroke"
                                        Value="{AppThemeBinding Light=Black, Dark=White}" />

                                </DataTrigger>

                                <!-- Cor não selecionada -->
                                <DataTrigger
                                    TargetType="Border"
                                    Binding="{Binding IsSelected}"
                                    Value="False">

                                    <Setter
                                        Property="Stroke"
                                        Value="Transparent" />

                                </DataTrigger>

                            </Border.Triggers>

                            <Border
                                BackgroundColor="{Binding Color}"
                                StrokeThickness="0"
                                StrokeShape="RoundRectangle 15">

                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}},
                                        Path=BindingContext.SelectColorCommand}"
                                        CommandParameter="{Binding .}" />
                                </Border.GestureRecognizers>

                            </Border>

                        </Border>

                    </DataTemplate>
                </CollectionView.ItemTemplate>

            </CollectionView>
            <HorizontalStackLayout
                HorizontalOptions="End"
                Spacing="10">

                <Button
                    Text="Cancelar"
                    Command="{Binding CancelCommand}" />

                <Button
                    Text="Salvar"
                    Command="{Binding SaveCommand}" />

            </HorizontalStackLayout>
        </VerticalStackLayout>
    </Border>
</toolkit:Popup>